ANSWER_1: The Course Materials Portal failed because the application was unable to read its configuration file at /etc/course-portal/portal.conf due to a Permission denied error.
ANSWER_2: The file /etc/course-portal/portal.conf has permissions -rw------- (octal 600), granting read and write permissions to the owner root (rw- / 6), no access to the group course-portal (--- / 0), and no access to others (--- / 0). Because the service executes under the course-portal user account, which is not the owner root but belongs to the course-portal group, the operating system applies group permissions (---) and denies read access.
ANSWER_3: 640
ANSWER_3_WHY: Option 400 (r--------) keeps group permissions as '---' (0), so the course-portal group still cannot read the file, while also stripping root's write permission. Option 755 (rwxr-xr-x) grants unnecessary execute permissions on a configuration file to root and group, while exposing read and execute permissions to all other users ('others'). Option 777 (rwxrwxrwx) violates the principle of least privilege by granting write and execute access to everyone ('others'), allowing any unprivileged user or attacker on the system to tamper with, overwrite, or execute the configuration file.
ANSWER_4_ORDER: B, G, E, D, F, A, I, C, H
ANSWER_5: Using chmod 777 grants write and execute permissions to everyone (world-writable), which allows any unauthorized user or attacker on the system to tamper with, overwrite, or inject malicious settings into portal.conf.
ANSWER_6: Sending an HTTP request to the Course Materials Portal (such as via curl http://localhost:<port>/ or a web browser) and receiving an HTTP 200 OK response with the expected portal page content, along with new application log entries in /var/log/course-portal/app.log confirming successful startup and request handling without permission errors.
ANSWER_7_BRIDGE: component=local configuration storage, detect=automated health check probes and error log monitoring, recover=automated configuration drift remediation or rollback, proof=end-to-end synthetic HTTP transactions returning 200 OK
